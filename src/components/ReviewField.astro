---
interface Props { value: string; label?: string; class?: string; }
const { value, label = 'Review field', class: className = '' } = Astro.props;
---
<p class:list={['review-field', className]}><span class="sr-only">{label}: </span>{value}</p>
